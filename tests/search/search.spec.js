import search, { __RewireAPI__ } from './../../src/search';

describe('src/search', () => {
    let expected, actual;
    beforeEach(() => {
        expected = undefined;
        actual = undefined;
    });
});